#include<iostream>
using namespace std;
int isprime(int n)
{
    for(int i=2;i<=n/2;i++){
        if(n%i==0){
            return 0;
        }
    }
    return 1;
}
void primecheck(int num){
    for(int i=2;i<=num;i++){
        if(isprime(i))
        {
            cout<<i<<" ";
        }
    }
}
int main(){
    int num;
    cin>>num;
    primecheck(num);
}
